# T10/T11/T14 evidence — 2026-09-13 (worker ses_f634e8e33ffeTlu6PdIqHs836U, claim T15)

## T10 — OpenCode/version/manifest compatibility truth

- Real binary: `opencode --version` → `1.18.30` (branch `feat/ocae-product-consolidation`).
- Manifest edit (`ecosystem.manifest.json`, only change):
  `supported_runtimes.opencode.min` `1.15.0` → `1.18.0`,
  `tested` `1.15.13` → `1.18.30`.
- Justification: min 1.15.0 is unverifiable — `test/compatibility/opencode-native-compatibility.test.mjs`
  asserts command-effect classification only (11 tests, zero version assertions).
  Evidenced floor is the 1.18.x line: `docs/ocae-cli.md` already describes the
  "installed OpenCode 1.18.x global plugin directory" discovery path, and `1.18.30`
  was verified live on this host. So min=`1.18.0` (docs-evidenced line floor),
  tested=`1.18.30` (execution-evidenced).
- Agent discovery (verified live):
  - Primary agents: `opencode.jsonc` `"agent"` entries with `"mode": "primary"`
    (`issue-orchestrator`, `plan`, `build`); subagents (`executor`, `review-agent`, …).
  - Installable file agents: `.opencode/agents/` (13 × `.md`, no swarm — swarm is not installable).
  - Swarm primary: `.agents/skills/coordinate-blackboard-swarm/adapters/opencode/swarm.md`.
  - swarm-worker: `.../adapters/opencode/swarm-worker.md`.
  - Skill: `.agents/skills/coordinate-blackboard-swarm/SKILL.md` (+ project skills under `.opencode/skills/`).
  - Custom swarm tool: `.../adapters/opencode/swarm.ts` (`tool({action: next|claim|fact|result|…})`,
    materialized by installer to `<target>/.opencode/tools/swarm.ts`,
    `swarm.md`→`.opencode/agents/swarm.md`, `swarm-worker.md`→`.opencode/agents/swarm-worker.md`
    per `scripts/install-governance.mjs`).
- No enforced code (mjs/py/json/jsonc, excl. frozen `docs/reports/`) references `1.15.0`/`1.15.13`
  after the edit (rg clean). Frozen reports are historical evidence, untouched.
- `test/compatibility/opencode-native-compatibility.test.mjs`: 11/11 PASS.

## T11 — Release/build provenance hardening

- `build_backend.py`: added `verify_release_consistency(*, manifest_version, tag, dirty,
  payload_manifest, archive_sha256)` — pure deterministic gate; FAILS (RuntimeError) on
  manifest≠tag (leading `v` stripped), dirty worktree, payload `package_version` drift,
  or `archive_sha256` mismatch. Added `check_release_version(manifest_version=None, tag=None)`
  wrapper reading live reality (manifest file, `OCAE_RELEASE_TAG` or exact-match git tag,
  `_relevant_worktree_status()`, built payload manifest+archive when present) plus
  CLI `python3 build_backend.py check-release --manifest-version X --tag Y` (exit 2 + `FAIL` line).
- Release-path wiring: `_prepare_payload()` runs the version+dirty gate when `OCAE_RELEASE_TAG`
  is set (pre-write, so no stale-payload false positives); post-build hash agreement is
  verified via `check-release`. No existing build path behavior changes otherwise.
- New `test/install/release-version-check.test.mjs`: 6/6 PASS (match passes; version drift,
  dirty tree, hash mismatch, payload-version drift fail; CLI drift fails closed).

## T14 — Documentation truth (minimal, contradiction-only)

1. `README.md` swarm file list: added `adapters/opencode/install.py`,
   `references/protocol.md`, `agents/openai.yaml` (payload members previously unlisted).
2. `README.md` authority one-liner: "The Blackboard may prioritize work but never grants
   authority; Governance V2 authorizes effects." (matches manifest `swarm` catalog entry).
3. `docs/ocae-cli.md` Requirements: `OpenCode >= 1.18.0 (verified against 1.18.30; matches
   supported_runtimes.opencode)` — README's "supported range documented in the CLI reference"
   pointer now resolves to a stated range.

## Verification

- `node --test test/install/release-version-check.test.mjs` → 6/6 PASS.
- `node --test test/compatibility/opencode-native-compatibility.test.mjs` → 11/11 PASS.
- `scripts/validate-ecosystem.mjs` → TIMEOUT (>120 s, full-suite sentinel; noted, not caused by
  this change — manifest parses OK, rg shows no stale enforced version pins).
- `git diff --stat` scope: `ecosystem.manifest.json`, `build_backend.py`,
  `test/install/release-version-check.test.mjs` (new), `README.md`, `docs/ocae-cli.md`,
  plus this evidence file. No commit/push. NOT marked DONE per task.
