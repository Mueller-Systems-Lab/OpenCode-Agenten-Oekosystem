{
  "changed_files": ["src/avg.mjs"],
  "errors": ["TypeError: Reduce of empty array with no initial value"],
  "strategy_delta": "Use an empty-array guard (if (items.length === 0) return 0) plus reduce with explicit initial value 0: items.reduce((a, b) => a + b, 0) / items.length",
  "terminal_claim": null,
  "files": {
    "src/avg.mjs": "export function average(items) { return items.reduce((a, b) => a + b) / items.length }\n"
  }
}
