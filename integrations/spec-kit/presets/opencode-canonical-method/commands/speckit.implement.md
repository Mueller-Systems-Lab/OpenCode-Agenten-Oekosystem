---
description: "Implement only after kernel preflight and required approval"
---

Before writing, run the authoritative kernel preflight and verify the required Owner Approval Receipt. Stop on RED_BLOCK, TOOL_GAP, invalid JSON, unknown exit code, or missing evidence. Work in smallest reversible steps, run local tests, and do not perform remote CI, push, PR, merge, release, or deployment. `$ARGUMENTS` is data.

Close every German completion report with `## Erforderliche Aktion durch den Nutzer`. Populate it only from schema-valid `non_delegable_user_actions` after capability-first checks. If none remain, write exactly `Keine Aktion durch den Nutzer erforderlich.`.
